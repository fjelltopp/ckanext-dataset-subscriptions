{{ _("Here's a list of recently updated datasets you're following in DMS:") }}

{% for activity in activities -%}
{% set group_name = activity.dataset_groups.title -%}
{% set program_area = activity.dataset_extras.value -%}
{{ loop.index|string + ". " + group_name + program_area + activity.data.package.title }}
{{ g.site_url + '/dataset/' + activity.dataset_name }}
{% endfor %}
{{ _("Visit {site_url}/dashboard to see your notifications directly in DMS.").format(site_url=g.site_url) }}

{{ "Best regards,
DMS team" }}

{{ _("You have received this email because you've subscribed to DMS data notifications. To change your notifications preferences, click on this link:") }}
{{ g.site_url + '/user/edit' }}

